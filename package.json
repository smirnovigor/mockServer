{
  "name": "mockServer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "description": "To run mock-server in different environment use SET NODE_ENV command (e.g. SET NODE_ENV=test&node server.js)",
    "start": "SET NODE_ENV=development&node server.js"
  },
  "dependencies": {
    "underscore": "^1.6.0",
    "express": "^4.5.1",
    "morgan": "^1.1.1",
    "body-parser": "^1.4.3",
    "serve-favicon": "^2.0.1"
  }
}
